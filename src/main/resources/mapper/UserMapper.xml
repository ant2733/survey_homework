<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.example.survey.mapper.UserMapper">
    <resultMap id="UserLogin" type="org.example.survey.entity.User">

    </resultMap>


    <insert id="addUser">
        INSERT INTO user (user_name, pass_word)
        VALUES (#{userName}, #{passWord})
    </insert>
    <select id="selectByUserName" resultType="org.example.survey.entity.User">
        SELECT id, user_name, pass_word, status, create_time, update_time
        FROM user
        WHERE user_name = #{userName}
    </select>
    <select id="getUserId" resultType="java.lang.Long">
        SELECT id
        FROM user
        WHERE user_name = #{userName}
    </select>
</mapper>